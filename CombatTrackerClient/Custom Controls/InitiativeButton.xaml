<UserControl
    x:Class="CombatTrackerClient.InitiativeButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CombatTrackerClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="InitiativeGrid" PointerEntered="InitiativeGrid_PointerEntered" PointerExited="InitiativeGrid_PointerExited">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
			<RowDefinition Height="50"/>
			<RowDefinition Height="1*"/>
		</Grid.RowDefinitions>
        <TextBlock x:Name="Name" Text="Ender" Grid.Column="0" Foreground="#FFFFFEFE" FontSize="14.667" HorizontalAlignment="Left" VerticalAlignment="Center" Padding="15,0,0,0"/>
        <TextBlock x:Name="Initiative" Grid.Column="1" Foreground="#FFFFFEFE" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock x:Name="Next" Grid.ColumnSpan="2" Grid.Row="2" Text="&#xE0AD;" Foreground="#FFFFFEFE" FontSize="48" VerticalAlignment="Center" FontFamily="Segoe UI Symbol" HorizontalAlignment="Right"/>
	</Grid>
</UserControl>
