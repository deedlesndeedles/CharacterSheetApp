<UserControl
    x:Class="CombatTrackerClient.Custom_Controls.LoadItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CombatTrackerClient.Custom_Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" Width="300" Height="300">

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" Background="Black" Padding="9,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="textName" Margin="0" TextWrapping="Wrap" Text="Name" FontSize="24" VerticalAlignment="Top" Grid.ColumnSpan="2"/>
            <TextBlock x:Name="textLevel" Margin="0" TextWrapping="Wrap" Text="Lv " FontSize="24" Grid.Column="1" VerticalAlignment="Bottom"/>
            <TextBlock x:Name="textCampaign" Margin="0" TextWrapping="Wrap" Text="Campaign" FontSize="24" VerticalAlignment="Bottom"/>
        </Grid>
        <Image x:Name="image" Margin="0" Stretch="UniformToFill"/>

    </Grid>
</UserControl>
